// Escreva uma função chamada ePaisagem que
// recebe dois argumentos, largura e altura
// de uma imagem (number).
// Retone true se a imagem estiver no modo paisagem.

const ePaisagem = (alt, larg) => larg > alt

console.log(ePaisagem(500, 900))